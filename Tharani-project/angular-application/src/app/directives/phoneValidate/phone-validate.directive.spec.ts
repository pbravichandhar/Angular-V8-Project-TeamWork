import { PhoneValidateDirective } from './phone-validate.directive';

describe('PhoneValidateDirective', () => {
  it('should create an instance', () => {
    // const directive = new PhoneValidateDirective();
    // expect(directive).toBeTruthy();
  });
});
